[package]
name = "texpand-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
texpand-core = { path = "../texpand-core" }
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
toml = "1"
anyhow = "1"
